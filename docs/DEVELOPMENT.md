# Development Guide

This document describes local development setup and workflow for Boat Race Predictor.

## Quick Start

1. Create and activate a virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Update the committed `.env` template values for your local environment.
4. Run `pytest` to validate changes.
