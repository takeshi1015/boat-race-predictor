# API Reference

This project currently exposes internal Python modules used by `main.py`.

Public interfaces are documented in module docstrings and will be expanded as API endpoints are formalized.
